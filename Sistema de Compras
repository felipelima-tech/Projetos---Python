nome_cliente = input('Cliente:')

nome_produto = input('Produto:')

preco_produto = float(input('Preço do Produto:'))

qtd_produto = int(input('Quantidade de Produtos:'))

valor_bruto = preco_produto * qtd_produto

desconto = input('O cliente possui cupom de desconto ?')

porcentagem = valor_bruto * 0.10

if desconto == 'sim'.lower().strip():

    valor_bruto = valor_bruto - porcentagem

    print('10%''de desconto')

porcentagem_pix = valor_bruto * 0.05

porcentagem_cartão = valor_bruto * 0.02

forma_de_pagamento = input('Forma de pagamento:').lower().strip()

if forma_de_pagamento == 'pix':
    valor_bruto = valor_bruto - porcentagem_pix 
    print('5%'  'de desconto')
elif forma_de_pagamento == 'cartão':
    valor_bruto = valor_bruto + porcentagem_cartão 
    print('2%' 'de taxa')
else:
    valor_bruto

print(f'Valor final: R$ {valor_bruto :.2f}')
